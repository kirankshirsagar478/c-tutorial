#include <stdio.h>

int main(){
    int r=6;
    float pi=3.14;
    printf ("The are of Circle is %f \n", pi*r*r);
    int h=8;
    printf ("Area of the Cylinder is %f \n",pi*r*r*h);
    return 0;
}